// Root app entry for Expo — re-export the expo-router entry
// This file is required so Metro's default AppEntry (node_modules/expo/AppEntry.js)
// can import '../../App' successfully. Using expo-router, the router provides
// the real application entry point, so re-export it here.

export { default } from "expo-router/entry";
